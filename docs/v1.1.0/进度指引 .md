# SuperPhoto v1.1.0 — 进度指引

> 用途：跨会话/跨智能体的进度总控。任何人接手先读本文件 + `实施文档.md` + `已验证事实.md`。
> 状态标记：⬜ 未开始 / 🟡 进行中 / ✅ 已完成 / ⛔ 阻塞。更新时间统一写绝对日期。
> 创建：2026-09-19。

## 当前阶段

- **全部三波已完成并通过验证**（2026-09-19）。31 项测试全绿（OIDC 单元 24 + OIDC 路由 4 + job_settings 3），后端导入冒烟通过，app.js 语法校验通过，DB 迁移在真实库副本上验证（删视频列 + 建 oidc_identities，数据零丢失）。
- 编排实际执行：Wave 1（后端此前已完成，本轮补前端 A2/A3）→ Wave 2（C 用户管理 + D 异步，D 由子智能体并行）→ Wave 3（E1 OIDC 后端由主理人做，E2 前端由子智能体并行）。

## 总进度看板

| 波次 | 子任务 | 智能体 | 独占文件 | 状态 |
|---|---|---|---|---|
| W1 | 视频移除·后端 | A1 | config.py / jobs.py / api/main.py / worker/main.py / db.py / tests/test_job_settings.py | ✅ |
| W1 | 视频移除·前端 | A2 | web/index.html / web/app.js / web/*.css | ✅ |
| W2 | 用户管理修复 | C | api/main.py / common/auth.py / common/config.py / web/app.js | ✅ |
| W2 | 异步优化 | D | worker/main.py / common/jobs.py | ✅ |
| W3 | OIDC·后端 | E1 | common/oidc.py(新) / api/main.py / common/config.py / common/db.py / common/auth.py | ✅ |
| W3 | OIDC·前端 | E2 | web/index.html / web/app.js | ✅ |

## 细项清单

### Wave 1 — 移除视频（依赖：用户放行）
- [ ] A1-1 config.py 删 VIDEO_* / DEFAULT_VIDEO_QUOTAS / FFMPEG_BIN / FFPROBE_BIN 等
- [ ] A1-2 jobs.py 删 validate_upload_type/UPLOAD_TYPES、video 分支、estimate_video_output_bytes、TARGETS 的 1k
- [ ] A1-3 api/main.py 删 upload_looks_video/store_upload_stream/probe_video/VIDEO_FORMAT_NAMES 及各 video 分支与 import
- [ ] A1-4 worker/main.py 删 process_video_job、process_job/valid_output/claim_job 的 video 逻辑
- [ ] A1-5 DB 迁移：备份 jobs.db → 守卫式 DROP COLUMN（jobs/uploads 6 列 + users.video_quotas）→ 同步 *_COLUMNS/SCHEMA
- [ ] A1-6 tests/test_job_settings.py 删 4 个 video 用例，photo 用例通过
- [ ] A2-1 index.html 删上传类型/1k/mp4/webm 控件
- [ ] A2-2 app.js 删 video 猴补丁全段，简化为纯图片
- [ ] A2-3 CSS 清理视频专用样式
- [ ] W1 验收：导入冒烟 + pytest 通过；前端仅图片、无报错
- [ ] W1 主理人汇总 + 放行 W2

### Wave 2 — 用户管理 + 异步（依赖：W1 完成）
- [ ] C-1 访客额度前后端统一（消除 5/3 不一致）
- [ ] C-2 重构 admin_update_user 额度逻辑，image_quotas 为唯一源，清理 legacy 映射 bug
- [ ] C-3 change_password 成功后失效其它会话
- [ ] C-4 delete_account 用 clear_session；require_admin 用 ROLE_ADMIN；auth_me 去嵌套 connect
- [ ] C-5 rate_limit 空桶回收 + 注释多进程限制
- [ ] C-6 访客 daily/active 收敛到 config 常量
- [ ] D-1 图片取消检查点（process_job 检查 update_progress 返回）
- [ ] D-2 租约/心跳时长调优 + 心跳线程收尾复核
- [ ] D-3 estimate_processing_seconds photo 路径复核
- [ ] W2 验收：取消 processing 能尽快中止；改额度/改密/禁用使其它会话失效
- [ ] W2 主理人汇总 + 放行 W3

### Wave 3 — OIDC 接入（依赖：W2 完成）
- [ ] E1-1 新增 common/oidc.py（discovery/PKCE/state/nonce/token/验签/userinfo）
- [ ] E1-2 config.py 增 OIDC_* 环境变量，缺失即 OIDC_ENABLED=false
- [ ] E1-3 db.py 新增 oidc_identities 表；首登建号（password_hash 哨兵）
- [ ] E1-4 main.py 增 /api/auth/oidc/login 与 /callback；auth/me 暴露 oidc_enabled
- [ ] E1-5 复用 auth.py 会话机制
- [ ] E2-1 前端登录弹窗加 OIDC 入口（oidc_enabled 门控）
- [ ] E2-2 回调后刷新状态、错误 toast
- [ ] W3 验收：OIDC_ENABLED=false 零影响；true 时授权 URL/callback 逻辑正确（可 mock 单测）
- [ ] W3 主理人汇总

## 外部前提（用户负责，非本仓库，任务 2 端到端必需）
- [ ] Sub2 开 `oidc_provider.enabled` + 配 encryption_key/secret_pepper
- [ ] Sub2 生成并激活签名密钥（admin `POST /admin/oidc-provider/keys/rotate`）
- [ ] Sub2 注册 SuperPhoto client（admin `POST /admin/oidc-provider/clients`，回调 https）→ 得 client_id/secret
- [ ] 若 issuer 域名 ≠ `auth.taffy.edu.kg`，Sub2 解冻 issuer 常量

## 待用户决定项（不阻塞主线）
- [ ] 根目录上游 `inference_realesrgan_video.py` / `inputs/video/*` / `docs/anime_video_model.md` 是否删除
- [ ] Sub2 OP 通电由谁/何时执行

## 变更日志
- 2026-09-19：完成四项任务调研，核实 Sub2 OIDC 能力，确认 4 项用户决策，产出 实施文档/已验证事实/进度指引 三份文档。
- 2026-09-19：完成全部三波实施。
  - W1 前端 A2/A3：删净 app.js 整段 video 猴补丁（多层叠加结构逐一收敛为纯图片）、修复 admin 用户表因视频配额列导致的列错位真 bug、清理 CSS 孤儿样式 `.upload-type-label`。**偏差说明**：实施文档 A2-2 把 `queuePreupload/preuploadFile` 列入"待删 video 猴补丁"，实测它们是**图片预上传的核心链路**（当前 submit 走 upload_ids 路径），删除会破坏上传，故予以保留——此为对文档的合理修正。
  - W2 C 用户管理：GUEST_ACTIVE_QUOTA 收敛到 config 常量（替换 usage_summary/create_modern_batch/retry_job 三处硬编码）；admin_update_user 以 image_quotas 为唯一事实源、移除 legacy resolution/daily_image_quota 映射与 payload/serialize 字段；change_password 成功后删除该用户其它会话并重签当前；delete_account 改用 clear_session；require_admin 用 ROLE_ADMIN 常量；auth_me 用不需 DB 的 model_capabilities() 消除嵌套 connect；rate_limit 加空桶周期回收 + 多进程限制注释；前端 renderIdentity/applyRoleUi 访客额度统一读后端 usage.daily_quota。
  - W2 D 异步（子智能体）：LEASE_SECONDS 30→60、新增 HEARTBEAT_SECONDS=15；process_job 图片路径 4 处 update_progress 均检查返回值并抛异常提前中止（取消检查点）；经核实 finish_job 的 lease_owner+processing 归属校验保证被取消任务不会误标 failed。
  - W3 E1 OIDC 后端：新增 common/oidc.py（discovery/PKCE-S256/state/nonce/授权URL/token交换Basic/验签软可选/userinfo，自包含不 import config）；config.py 增 OIDC_* 环境变量（缺关键项 fail-closed）；db.py 增 oidc_identities 表；main.py 增 /api/auth/oidc/login 与 /callback（签名 flow-cookie 存 state/nonce/verifier，首登建号 password_hash 哨兵 'oidc$disabled'，复用 rotate_session）；auth/me 暴露 oidc_enabled。
  - W3 E2 OIDC 前端（子智能体）：登录弹窗加"使用 Personal_Sub2 登录"入口（oidc_enabled 门控显隐）、点击跳转 /api/auth/oidc/login、boot 处理 ?oidc_error 并清 query。
  - 验证：新增 tests/test_oidc.py（24）+ tests/test_oidc_routes.py（4，端到端 mock），连同 test_job_settings.py 共 31 项全绿；DB 迁移真实库副本验证通过。
